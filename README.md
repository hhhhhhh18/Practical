# Practical

## Task-1

1) Fork the repository of "Sumukha47 /Practical" and clone it to desktop. 
2) Create 2 files linux.txt and script.sh inside the cloned folder
3) write "User management Commands (All commands)" 
    inside Linux.txt file and "Script to create users" inside script.sh 
    add these files and commit them individually.
4) Create a branch called as "surprise" and create a file called as
    aws.txt
5) Inside this file write about"AWS Global Infrastructure" add this file and 
    commit it.
6) In main branch, create a new file "file-permission.txt", in this file write about 
    file permission (ALL Theory and Commands) and commit it.
7) Merge surprise branch with master branch.
8) Rename the main branch as master in local machine.
9) Push all the branches and send me a pull request.

## Task-2

1) Create an instance and an S3 Bucket.
2) Upload the static website files in S3
    bucket
3) Connect EC2 and S3
4) Copy the files from S3 to EC2
5) Host the website
6) Copy the contents of the root volume
   and create another instance in different
   region using the copied volume.
7) Host the website in that other region as well.
